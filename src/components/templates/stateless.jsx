import React from 'react';
// import PropTypes from 'prop-types';

const propTypes = {

};

const defaultProps = {

};

const ChangeMe = () => (
  <div>
    Change Me Too!
  </div>
);

ChangeMe.propTypes = propTypes;
ChangeMe.defaultProps = defaultProps;

export default ChangeMe;
