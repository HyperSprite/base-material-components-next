TagBar example:

No tags
```
<TagBar />
```

Tags as strings
```
<TagBar tagList={'new,friends,eat,cookies'} />
```

Tags as an array
```
<TagBar tagList={['new','friends','eat','cookies']} navClick />
```

Closing tag
```
<TagBar qTag="car" />
```
